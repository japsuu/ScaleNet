namespace ScaleNet.Common
{
    public static class SharedConstants
    {
        // Networking
        public const int MAX_PACKET_SIZE_BYTES = 2048;
        public const int MAX_MESSAGE_SIZE_BYTES = MAX_PACKET_SIZE_BYTES - 2;
    }
}